// Re-export depuis le composant principal (avec sidebar + navigation complète)
export { default } from "@/Components/Layout/AppLayout";
export { AppLayout, useTheme } from "@/Components/Layout/AppLayout";
